## This file was auto-generated by CloudCoreo CLI
## this yaml file contains variables, their defaults, overrides, type and namespaces. i.e:
## variables:
##   PUBLIC_ROUTE_NAME:
##     default: my-public-route
##     description: 'the name to give to the public route'
##     required: true
##     type: string

variables:
  VPN_KEY_BUCKET:
    default: cyvpnk
    description: the bucket we should store the keys in
    overrides:
    required: true
    type:
  VPN_KEY_BUCKET_REGION:
    default: us-west-2
    description: the region where the vpn key bucket is located
    overrides:
    required: true
    type:
  VPN_SSH_KEY_NAME:
    default: Cy_Key
    description: the key pair we can use to ssh into boxes
    overrides:
    required: true
    type:
  NAT_KEY:
    default: Cy_Key
    description: the key pair we can use to ssh into boxes
    overrides:
    required: true
    type:
  VPN_DNS_PREFIX:
    default: cy-vpn
    description: the dns entry to create for the VPN server (<prefix>.<zone>)
    overrides:
    required: true
    type:
  BACKUP_BUCKET:
    default: cybu
    description:
    overrides:
    required: true
    type:
  DNS_ZONE:
    default: cy.cloudcoreo.com
    description:
    overrides:
    required: true
    type:
  ENV:
    default: dev
    description:
    overrides:
    required: true
    type:
  REGION:
    default: us-east-1
    description:
    overrides:
    required: true
    type:
  DATADOG_KEY:
    default: NA
    value:
    description: If you have a datadog key, enter it here and we will install the agent
    overrides:
    required: false
  PUBLIC_SUBNET_NAME:
    required: true
    description: the cloudcoreo name of the public vpc subnets
    default: cy-public-subnet
    type: string
  PUBLIC_ROUTE_NAME:
    required: true
    description: the name to give to the public route
    default: cy-public-route
  PRIVATE_SUBNET_NAME:
    required: true
    description: the cloudcoreo name of the private vpc subnets
    default: cy-private-subnet
  PRIVATE_ROUTE_NAME:
    required: true
    description: the name to give to the private route
    default: cy-private-route
  PRIVATE_SUBNETS:
    default: "STACK::coreo_aws_vpc_subnet.cy-private-subnet.subnet_ids"
    required: true
    descripiton: comma seperated list of the private subnet ids
  VPN_INSTANCE_TYPE:
    default: t2.micro
    description: the instance type for  the VPN_INSTANCE
    overrides:
    required: true
    type:
  VPC_NAME:
    default: cy-test-vpc
    description: the name of the VPC
    required: true
  VPC_OCTETS:
    default: 10.0.0.0
    description: the name of the VPC
    required: true
  ZONE:
    default: cy.cloudcoreo.com
    description: the dns zone
    required: true
  VPN_AMI_ID:
    default: ami-08842d60
    description: the AMI id to use for the vpn client
    overrides:
    required: true
    type:
  NAT_AMI:
    required: true
    description: the ami id of the nat
    default: "ami-4c9e4b24"
